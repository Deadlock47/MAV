{
  "name": "backend",
  "version": "1.0.0",
  "description": "```bash pip install -r requirements.txt python -m app.sync_duckdb uvicorn app.main:app --reload ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "electron": "^42.3.0"
  }
}
